#include<stdio.h>
    int main()
    {
        int n;
        scanf("%d",&n);
        printf("%d\n",fib(n));
        return 0;
    }
    
    int fib(n)
    {
        if(n==0)
        {
            return 0;
        }
        else
    }
